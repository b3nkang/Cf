soit a = 7;
soit b = 9;
soit c = 15;

si (a + b < c) {
    si (a - b > 0) {
        rendre (c - a) + b;
    } sinon {
        rendre b + c;
    }
} sinon {
    si (a + c > b * 4) {
        rendre (a + b) + c;
    } sinon {
        si (c + 1 == a + b) {
            si (b - a - 2 >= 0) {
                rendre ((a * b) + c) / 2;
            } sinon {
                rendre (a + c) + b;
            }
        } sinon {
            rendre (a + b + c) * 2 - 4;
        }
    }
}
