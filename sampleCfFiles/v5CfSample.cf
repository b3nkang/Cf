soit a = 7;
soit b = 9;
soit c = 15;

si (a + b < c) {
    si (a - b > 0) {
        retourner (c - a) + b;
    } sinon {
        retourner b + c;
    }
} sinon {
    si (a + c > b * 4) {
        retourner (a + b) + c;
    } sinon {
        si (c + 1 == a + b) {
            si (b - a - 2 >= 0) {
                retourner ((a * b) + c) / 2;
            } sinon {
                retourner (a + c) + b;
            }
        } sinon {
            retourner (a + b + c) * 2 - 4;
        }
    }
}
